import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const Babykeeping = () => {
  return (
    <SafeAreaView>
      <Text>Dang viec le</Text>
    </SafeAreaView>
  );
};

export default Babykeeping;
